services:
  - type: web
    name: pietro-emilly
    env: static
    buildCommand: echo "Static site ready"
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
